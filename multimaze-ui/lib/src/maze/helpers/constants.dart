/// Arbitrary initial value that UI elements were sized against.
///
/// This value is used to scale down various text elements as the window grows
/// or shrinks. There is nothing special about 1600, other than elements were
/// initially sized to be appropriate for that width.
const double defaultWindowWidth = 1600.0;
